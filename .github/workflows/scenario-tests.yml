name: Screenplay Tests

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: scenario-tests-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  scenario-dwm:
    name: DWM Screenplay scenarios
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XVFB and Mesa
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            xvfb \
            x11-xserver-utils \
            libgl1-mesa-dri \
            libgl1 \
            libasound2t64 \
            libx11-6 \
            libxext6 \
            libxrender1 \
            libxtst6 \
            libxi6

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run DWM Screenplay tests
        working-directory: dwm
        run: |
          chmod +x ./gradlew
          ./gradlew runScreenplayTests \
            -PscreenplayDisplay=xvfb \
            -PscreenplayTimeout=120
        env:
          _JAVA_OPTIONS: -Xmx3200m
          GRADLE_OPTS: -Dorg.gradle.daemon=false

      - name: Upload DWM scenario artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenplay-tests-dwm
          path: |
            dwm/build/screenplay/results/
            dwm/build/screenplay/report.xml
            dwm/build/screenplay/metrics.json
            dwm/build/screenplay/diagnostics.txt
            dwm/build/screenplay/run/screenshots/
            dwm/build/screenplay/vanilla-server/logs/
          if-no-files-found: ignore

  scenario-screenplay:
    name: Screenplay library demos
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XVFB and Mesa
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            xvfb \
            x11-xserver-utils \
            libgl1-mesa-dri \
            libgl1 \
            libasound2t64 \
            libx11-6 \
            libxext6 \
            libxrender1 \
            libxtst6 \
            libxi6

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run Screenplay library demo tests
        working-directory: screenplay
        run: |
          chmod +x ./gradlew
          ./gradlew runScreenplayTests \
            -PscreenplayDisplay=xvfb \
            -PscreenplayTimeout=120
        env:
          _JAVA_OPTIONS: -Xmx3200m
          GRADLE_OPTS: -Dorg.gradle.daemon=false

      - name: Upload Screenplay library artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenplay-tests-library
          path: |
            screenplay/build/screenplay/results/
            screenplay/build/screenplay/report.xml
            screenplay/build/screenplay/metrics.json
            screenplay/build/screenplay/diagnostics.txt
            screenplay/build/screenplay/run/screenshots/
            screenplay/build/screenplay/vanilla-server/logs/
          if-no-files-found: ignore

  compare-perf:
    name: Compare Screenplay performance
    needs: scenario-dwm
    if: always() && github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download current Screenplay artifacts
        uses: actions/download-artifact@v4
        with:
          path: perf/current
          pattern: screenplay-tests-*
          merge-multiple: false

      - name: Download latest green main baseline artifacts
        id: baseline
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p perf/baseline
          echo '_none yet_' > perf/baseline-label.txt
          RUN_JSON="$(gh run list \
            --workflow=scenario-tests.yml \
            --branch=main \
            --status=success \
            --limit=1 \
            --json databaseId,url)"
          RUN_ID="$(echo "$RUN_JSON" | jq -r '.[0].databaseId // empty')"
          RUN_URL="$(echo "$RUN_JSON" | jq -r '.[0].url // empty')"
          if [[ -z "$RUN_ID" ]]; then
            echo "No successful main Screenplay Tests run found yet."
            exit 0
          fi
          printf 'run [#%s](%s) on `main`\n' "$RUN_ID" "$RUN_URL" > perf/baseline-label.txt
          gh run download "$RUN_ID" -D perf/baseline || true

      - name: Compare metrics (advisory)
        run: |
          python3 .github/scripts/compare-scenario-perf.py \
            --current-dir perf/current \
            --baseline-dir perf/baseline \
            --markdown-out perf/comment.md \
            --baseline-label "$(cat perf/baseline-label.txt)"

      - name: Upsert PR performance comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- dwm-screenplay-perf -->';
            const body = fs.readFileSync('perf/comment.md', 'utf8');
            if (!body.includes(marker)) {
              throw new Error('compare output missing marker ' + marker);
            }
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number,
              per_page: 100,
            });
            const existing = comments.find((comment) =>
              comment.body && comment.body.includes(marker)
            );
            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body,
              });
              core.info(`Updated existing Screenplay perf comment ${existing.id}`);
            } else {
              const created = await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body,
              });
              core.info(`Created Screenplay perf comment ${created.id}`);
            }
